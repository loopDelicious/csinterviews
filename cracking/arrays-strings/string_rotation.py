"""assume a method isSubstring which checks if one word is a substring of another.
given 2 strings, check if s2 is a rotation of s1 using only one call to isSubstring

>>> is_rotation("waterbottle", "erbottlewat")
True

"""

def is_rotation(str1, str2):
    """check if str2 is a rotation of s1 using only one call to isSubstring"""




if __name__ == '__main__':
    import doctest
    if doctest.testmod().failed == 0:
        print "\n*** ALL TESTS PASSED. BONER!\n"